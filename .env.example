# === RMSG Private Chat – Example Env ===

# CORS: orígenes permitidos (separados por coma)
CORS_ALLOW_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,*

# (añade aquí más variables reales cuando vayas integrando)
